{
  "name": "@vgzt/contact-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "types": "wrangler types",
    "types:check": "wrangler types --check",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "config:check": "wrangler deploy --dry-run --outdir dist",
    "check": "wrangler types && tsc --noEmit && vitest run && wrangler deploy --dry-run --outdir dist",
    "deploy:dry-run": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.21.3",
    "@types/node": "^24.10.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.123.0"
  },
  "packageManager": "pnpm@11.19.0"
}
